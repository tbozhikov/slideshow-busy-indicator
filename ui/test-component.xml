<GridLayout id="root" visibility="{{ isBusy ? 'visible' : 'collapsed' }}" backgroundColor="{{backgroundColor}}" class="test" >
    <!--<GridLayout class="background-layer"></GridLayout>-->
    <GridLayout id="indicator" backgroundColor="{{ color }}" width="80" height="80" style="margin: 20">
        <GridLayout width="40" height="40">
            <Image id="image1" src="{{imgSource1}}"></Image>
            <Image id="image2" src="{{imgSource2}}"></Image>
        </GridLayout>
    </GridLayout>
</GridLayout>