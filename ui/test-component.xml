<GridLayout visibility="{{ isBusy ? 'visible' : 'collapsed' }}" style="background-color: red;" class="test" >
    <Label text="{{ lblText }}" />
    <!--<GridLayout class="background-layer"></GridLayout>-->
    <GridLayout width="80" height="80" style="background-color: green; margin: 20">
        <GridLayout width="40" height="40">
            <!--<Image [@image1State]="stateSwitch ? 'active' : 'inactive'" [@rotateStates]="stateSwitch" src="{{imgSource1}}"></Image>
            <Image [@image2State]="!stateSwitch ? 'active' : 'inactive'" [@rotateStates]="!stateSwitch" src="{{imgSource2}}"></Image>-->
            <Image id="image1" src="{{imgSource1}}"></Image>
            <Image id="image2" src="{{imgSource2}}"></Image>
        </GridLayout>
        <!--<ImageTransitionControl [content]="signal" [images]="images" width="40" height="40"></ImageTransitionControl>-->
    </GridLayout>
</GridLayout>