<GridLayout visibility="{{ isBusy ? 'visible' : 'collapsed' }}" >
    <GridLayout backgroundColor="{{ backColor }}" opacity="{{ backOpacity }}"></GridLayout>
        <GridLayout width="{{ indicatorWidth }}" height="{{ indicatorHeight }}" >
            <GridLayout backgroundColor="{{ indicatorColor }}" 
                opacity="{{ indicatorOpacity }}" 
                borderRadius="{{ indicatorBorderRadius }}">
            </GridLayout>

            <Image id="image1" src="{{ imgSource1 }}" margin="20"></Image>
            <Image id="image2" src="{{ imgSource2 }}" margin="20"></Image>
        </GridLayout>
</GridLayout>